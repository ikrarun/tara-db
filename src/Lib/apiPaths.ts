import { host } from "./host";

const get_all_post = `${host}/api/get_all_post`;
const get_quotes = `${host}/api/get_quotes`;
const get_suggested_book = `${host}/api/get_suggested_book`;

export { get_all_post, get_quotes, get_suggested_book };
